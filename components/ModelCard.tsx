import Link from "next/link";import type {ShoeModel} from "@/types/model";
export default function ModelCard({model}:{model:ShoeModel}){return <article className="card"><div className="card-image">{model.photos?.[0]?<img src={model.photos[0]} alt={model.name||model.code}/>:<span className="muted">بدون تصویر</span>}</div><div className="card-body"><div className="code">{model.code}</div><div className="muted">{model.name||"بدون نام"}</div><div className="card-actions"><Link className="btn btn-primary" href={`/models/${encodeURIComponent(model.code)}`}>مشاهده</Link><Link className="btn btn-secondary" href={`/models/${encodeURIComponent(model.code)}?edit=1`}>ویرایش</Link></div></div></article>}